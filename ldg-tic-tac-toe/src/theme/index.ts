import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      default: "#1A2A33",
    },
    primary: {
      main: "#31C3BD",
      dark: "#65E9E4",
    },
    secondary: {
      main: "#F2B137",
      dark: "#FFC860",
    },
  },
});

export default theme;
